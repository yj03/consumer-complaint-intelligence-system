# Trained Models

The trained `.pkl` model files are not stored in GitHub because of
their size.

Run the training scripts inside the `notebooks` folder to regenerate:

- `product_classifier.pkl`
- `issue_classifier.pkl`
- `timely_response_classifier.pkl`
- `improved_topic_model.pkl`
- `improved_topic_vectorizer.pkl`
